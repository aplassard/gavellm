[project]
name = "gavellm"
version = "0.1.0"
description = "Open source LLM-as-a-judge"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
